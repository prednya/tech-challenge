# Database Configuration
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/assistant

# AI Service Configuration (Choose one or simulate)
OPENAI_API_KEY=your-openai-key-here
ANTHROPIC_API_KEY=your-anthropic-key-here
AI_MODEL=gpt-3.5-turbo
# Alternative: Use "simulate" for mock AI responses
AI_PROVIDER=simulate

# Application Settings
SECRET_KEY=your-secret-key-here
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# CORS Settings
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Session Configuration  
SESSION_TIMEOUT_HOURS=4
MAX_CONTEXT_SIZE=100

# Rate Limiting
RATE_LIMIT_PER_MINUTE=100
MAX_SSE_CONNECTIONS=50

# Frontend Environment Variables
REACT_APP_API_URL=http://localhost:8000
REACT_APP_WS_URL=ws://localhost:8000

# Docker Development
POSTGRES_DB=assistant
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# Optional seeding
# When set to true, the backend will attempt to insert the full sample catalog
# on startup. Default is false (no automatic seeding).
SEED_ON_STARTUP=false
# Disable app-driven seeding. Postgres entrypoint will run init.sql on first boot.
SEED_FROM_SQL=false
